# Functions topology placeholder

This directory defines only the isolated npm/deploy boundary approved by
SaaS-03B-B0-I-R2. It contains no Cloud Functions, Admin SDK, transport or
backend implementation.

The vendored `@mipymetic/saas-contracts` tarball is generated from the package
source, reviewed by inventory and checksum, and installed through this
directory's independent lockfile.
