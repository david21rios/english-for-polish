/**
 * Registration policy value object composed within TenantSettings.
 *
 * These flags describe policy only. They do not validate email addresses,
 * create invitations, approve requests or grant access.
 * Its source location supports the registration domain and does not imply
 * independent ownership or lifecycle.
 *
 * @typedef {object} RegistrationPolicy
 * @property {boolean} openRegistration
 * @property {boolean} invitationOnly
 * @property {boolean} institutionalEmailOnly
 * @property {boolean} manualApprovalRequired
 */

export {};
