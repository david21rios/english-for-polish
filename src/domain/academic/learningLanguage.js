/**
 * Language taught by a course.
 *
 * @typedef {object} LearningLanguage
 * @property {string} languageCode Canonical BCP 47 language code.
 * @property {string} displayName Human-readable language name.
 */

export {};
