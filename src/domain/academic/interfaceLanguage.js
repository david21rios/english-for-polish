/**
 * Language used exclusively by the product interface.
 *
 * It does not identify the language taught by a course.
 *
 * @typedef {object} InterfaceLanguage
 * @property {string} locale BCP 47 interface locale.
 * @property {string} displayName Human-readable locale name.
 */

export {};
