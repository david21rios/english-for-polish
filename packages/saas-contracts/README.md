# @mipymetic/saas-contracts

Private, dependency-free ESM contracts shared by the browser application and
the future privileged backend. This package is not authorized for publication.

Domain exports are intentionally empty until SaaS-03B-B0-I-R3 migrates the
frozen Domain 1.2.0 implementation. Consumers must use only declared export
subpaths.
